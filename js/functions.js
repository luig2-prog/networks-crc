const nDigitG = (g) => { 
    //Número de digitos que tiene G
    return g.length;
}

const nDigitDR = (d , r) => { //Cantidad de digitos que tiene D + R
    const arrayDigitDR = d + r;
    return arrayDigitDR.length;
}



const calcularCrc = (d, g, r) => {


}